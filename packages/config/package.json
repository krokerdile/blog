{
  "name": "@hyunu/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared configurations for linting, formatting, etc.",
  "main": "index.js",
  "files": [
    "eslint",
    "prettier"
  ],
  "scripts": {
    "clean": "echo 'No build artifacts to clean'"
  }
}
